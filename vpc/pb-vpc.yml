---
 - hosts: localhost
   connection: local
   gather_facts: false
   user: root
   pre_tasks:
    # For "ansible-playbook -vv -i localhost, -e "type=all" pb-vpc.yml"
    # command usage:
    # - include_vars: ec2_vars/{{type}}.yml
    - include_vars: ec2_vars/all.yml
   roles:
    - vpc
